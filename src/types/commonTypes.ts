export enum UnitType {
  PLAYER_CONTROLLED,
  AI,
}
export enum UnitSubType {
  MELEE,
  RANGED_LOS,
  RANGED_RADIUS,
  SUPPORT_CLASS,
  PLAYER_CONTROLLED,
}

export enum Faction {
  ALLY,
  ENEMY,
}
