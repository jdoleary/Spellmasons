How pathfinding works:
- findPaths
    - tryPaths
        - for each path
            - finish OR
            - branch
                - walkAroundPoly (may add a bunch of points)

```js
    // Temp for dev:
    // Find duplicate points
    for (let path of paths) {
        let lastPoint;
        for (let point of path.points) {
            if (!lastPoint) {
                lastPoint = point;
            } else {
                if (Vec.equal(lastPoint, point)) {
                    console.log('duplicate point found');

                }

            }

        }

    }

```